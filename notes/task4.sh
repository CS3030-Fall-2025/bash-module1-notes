#! /usr/bin/bash
# Set dir to HOME/bin


# Check if directory already exists
# If it exists, move into it and print out message that it exists
# -d of ls is for directory

# if directory does not exist, create the directory and move to it
