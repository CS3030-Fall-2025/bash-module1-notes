#! /usr/bin/bash

#Write a script that creates a directory called bpl inside of $HOME.
#Populate this directory with two subdirectories called: bin and scripts
  # change to the directory of $HOME
 # create the bpl directory
# change to the directory bpl
 # create the two subdirectories of bin and scripts
 # show subdirectories

# Remove the subdirectories we created
# move up a directory
# show directories
