#! /usr/bin/bash
# set number and clue values

#Test while loop
#Syntax: While <condition>
# do
#   while ($num < $clue) {print it; $num++}
    # when variables are number, we will use (()) instead of [[]]
    # increment number
 
